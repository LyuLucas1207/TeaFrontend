
import React from 'react';
import { validateUrl } from '../utility/validate';
function Products() {
    return (
        <div>
            <h1>Product</h1>
        </div>
    );
}

export default validateUrl(Products);