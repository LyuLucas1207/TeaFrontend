
import React from 'react';
import { validateUrl } from '../utility/validate';
function About() {
    return (
        <div>
            <h1>About</h1>
        </div>
    );
}

export default validateUrl(About);